apply plugin: 'com.android.application'

android {
    compileSdkVersion propCompileSdkVersion

    defaultConfig {
        versionCode 1
        versionName "1.0"

        minSdkVersion 14
        targetSdkVersion 28
    }
}

dependencies {
    compile project(':ChangeLog-dialog')
    compile 'com.android.support:support-v4:28.0.0'
}


