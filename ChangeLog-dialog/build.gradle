apply plugin: 'com.android.library'

android {
    compileSdkVersion propCompileSdkVersion

    defaultConfig {
        versionName libraryVersion
        minSdkVersion 14
        project.archivesBaseName = "ChangeLog"
    }
}

dependencies {
    implementation project(':ChangeLog-core')
}
